apiVersion: v1
kind: Service
metadata:
  name: dialogic-web
spec:
  clusterIP: None  # headless — required governing service for StatefulSet
  ports:
    - protocol: TCP
      port: {{ .Values.web.service.port }}
      targetPort: {{ .Values.web.service.port }}
  selector:
    app: dialogic-web
